<Window x:Class="ITService.Backup.AppDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Border x:Name="RootBorder" CornerRadius="14" BorderThickness="1"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            Padding="0" MinWidth="360" MaxWidth="500">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.55" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="AccentBar" Grid.Row="0" CornerRadius="14,14,0,0"/>

            <Grid Grid.Row="1" Margin="20,16,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="IconBadge" Width="40" Height="40" CornerRadius="10" Margin="0,0,14,0">
                    <TextBlock x:Name="IconText" FontSize="18" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" TextWrapping="Wrap"/>
                    <TextBlock Text="ITBackup" FontSize="10" Foreground="{StaticResource MutedBrush}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Margin="20,14,20,0">
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="13" LineHeight="20"
                           Foreground="{StaticResource TextBrush}"/>
                <Border x:Name="LogPathPanel" Margin="0,12,0,0" Padding="10,8"
                        Background="{StaticResource InputBgBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="8" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Папка логов" FontSize="10" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4"/>
                        <TextBlock x:Name="LogPathText" TextWrapping="Wrap" FontSize="11"
                                   Foreground="{StaticResource PrimaryGlowBrush}"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="20,18,20,18">
                <Button x:Name="SecondaryButton" Style="{StaticResource SecondaryButton}"
                        Click="Secondary_Click" Margin="0,0,10,0" MinWidth="100" Visibility="Collapsed"/>
                <Button x:Name="PrimaryButton" Style="{StaticResource PrimaryButton}"
                        Click="Primary_Click" MinWidth="100"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
