<Window x:Class="ITService.Backup.OneCImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт из 1С — ITBackup"
        Icon="/Assets/app.ico"
        Height="480" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Базы из списка 1С текущего пользователя" FontWeight="SemiBold" FontSize="15"/>
        <TextBlock Grid.Row="1" x:Name="SummaryText" TextWrapping="Wrap" FontSize="11"
                   Foreground="{StaticResource MutedBrush}" Margin="0,6,0,10"/>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="8">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="Выбрать все" Style="{StaticResource SecondaryButton}"
                            Click="SelectAll_Click" Padding="10,6" Margin="0,0,8,0"/>
                    <Button Content="Снять все" Style="{StaticResource SecondaryButton}"
                            Click="SelectNone_Click" Padding="10,6" Margin="0,0,8,0"/>
                    <Button Content="Указать файл .v8i…" Style="{StaticResource SecondaryButton}"
                            Click="BrowseV8iFile_Click" Padding="10,6"/>
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="PathsList"/>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}" Click="Cancel_Click"
                    Margin="0,0,10,0" IsCancel="True" MinWidth="100"/>
            <Button Content="Добавить выбранные" Style="{StaticResource PrimaryButton}"
                    Click="Import_Click" IsDefault="True" MinWidth="150"/>
        </StackPanel>
    </Grid>
</Window>
